vich_uploader:
    db_driver: orm

    metadata:
            type: attribute

    mappings:
        import_files:
            # uri_prefix: /test_images
            upload_destination: '%kernel.project_dir%/tests/Fixtures/Uploads'
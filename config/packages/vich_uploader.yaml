vich_uploader:
    db_driver: orm

    metadata:
            type: attribute

    mappings:
       import_files:
        #    uri_prefix: /products
           upload_destination: '%kernel.project_dir%/var/uploads'
           namer: App\Naming\ImportFileNamer
